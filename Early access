<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>IG Studios — Urban X</title>
  <style>
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%}
    body{
      font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background:#111;
      display:flex;
      align-items:center;
      justify-content:center;
      color:#fff;
      padding:20px
    }
    .hero{
      width:100%;
      max-width:1200px;
      height:700px;
      border-radius:14px;
      overflow:hidden;
      position:relative;
      box-shadow:0 20px 60px rgba(2,6,23,0.6);
      background-image: url('https://github.com/VinitProgrammer/UrbanX-website/blob/main/ChatGPT%20Image%20Sep%2026,%202025,%2004_04_25%20PM.png?raw=true'); /* apna image link yaha daalna */
    background-size: 100% 
      background-position:center;
      background-repeat:no-repeat;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      padding:100px;
    }

    /* dark overlay for contrast */
    .hero::before{
      content:'';
      position:absolute;
      inset:0;
      background:linear-gradient(rgba(3,10,18,0.5), rgba(3,10,18,0.6));
      pointer-events:none;
    }

    .brand{
      position:relative;
      z-index:2;
      display:flex;
      flex-direction:column;
      align-items:center;
      gap:18px
    }
    .studio{
      font-weight:700;
      letter-spacing:6px;
      font-size:36px;
      opacity:0.95
    }
    .title{
      font-size:160px;
      line-height:0.9;
      font-weight:800;
      text-transform:capitalize;
      margin-top: -10px;
      text-shadow: 0 2px 0 rgba(0,0,0,0.35);
      color:rgba(255,255,255,0.95)
    }

    /* responsive */
    @media (max-width:900px){
      .hero{height:520px}
      .title{font-size:96px}
      .studio{font-size:20px}
    }
    @media (max-width:520px){
      .hero{height:420px;padding:500px}
      .title{font-size:56px}
      .studio{font-size:16px}
    }

    /* Button */
    .btn{
      display:inline-block;
      text-decoration:none;
      padding:14px 36px;
      border-radius:12px;
      font-weight:800;
      letter-spacing:1px;
      transition:transform .16s ease, box-shadow .16s
    }
    .primary{
      background:#1565d8;
      color:#fff;
      box-shadow:0 10px 30px rgba(21,101,216,0.18)
    }
    .primary:hover{
      transform:translateY(-4px);
      box-shadow:0 18px 40px rgba(21,101,216,0.28)
    }
    .caption{
      position:relative;
      z-index:2;
      margin-top:16px;
      color:rgba(255,255,255,0.8);
      font-size:14px
    }
  </style>
</head>
<body>
  <main class="hero">
    <header class="brand">
      <div class="studio">IG STUDIOS</div>
      <h1 class="title">Urban X</h1>
      <a class="btn primary" href="store.html" id="buy-btn">BUY</a>
    </header>
    <p class="caption">A realistic, atmospheric urban promotional hero for your site.</p>
  </main>
</body>
</html>
